{
  "page": "string — which screen/page this issue was found on",
  "persona": "string — which persona flagged it (e.g. 'first_time_user')",
  "issue": "string — plain description of the problem",
  "severity": "string — one of: low, medium, high",
  "location": "string — where on the page (e.g. 'top nav', 'checkout button')",
  "suggestion": "string — the persona's recommended fix"
}